function ensureContract(){let day=Math.floor(Date.now()/86400000);if(S.contractDay===day&&S.contract)return;let rs=['common','rare','epic','legendary'],r=rs[(day+S.level)%rs.length],goal=r==='legendary'?1:r==='epic'?2:3;S.contract={rarity:r,goal,progress:0,reward:220+goal*90+S.level*20,done:false};S.contractDay=day;save()}function claimDaily(){let day=Math.floor(Date.now()/86400000);if(S.daily===day)return toast('Daily crate already claimed');let streak=S.daily===day-1?Math.min(7,(S.dailyStreak||0)+1):1,reward=120+streak*55;S.daily=day;S.dailyStreak=streak;S.cash+=reward;S.energy=S.maxEnergy;save();beep(900,.16);renderAll();toast('DAILY CRATE +'+money(reward))}
function offlineReward(){let now=Date.now(),elapsed=Math.max(0,now-(S.lastSeen||now)),mins=Math.floor(elapsed/60000);regenEnergy();if(mins>=10){let cap=Math.min(mins,480),gain=Math.floor(cap*(2+S.up.bench*.7));S.cash+=gain;S.lastSeen=now;save();setTimeout(()=>toast('OFFLINE EARNINGS +'+money(gain)+' • energy '+S.energy+'/'+S.maxEnergy),400)}}
function checkAchievements(){let vals={first:S.stats.sold,five:S.stats.sold,profit1k:S.stats.profit,perfect:S.stats.perfect,level5:S.level};let bonus=0;for(let a of ACH){if(!S.ach[a[0]]&&vals[a[0]]>=a[3]){S.ach[a[0]]=true;bonus+=150}}if(bonus){S.cash+=bonus;save();toast('ACHIEVEMENT BONUS +'+money(bonus));beep(980,.15)}}
